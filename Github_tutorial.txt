{\rtf1\ansi\ansicpg1252\cocoartf2580
\cocoatextscaling0\cocoaplatform0{\fonttbl\f0\fnil\fcharset0 HelveticaNeue;}
{\colortbl;\red255\green255\blue255;}
{\*\expandedcolortbl;;}
\margl1440\margr1440\vieww11520\viewh8400\viewkind0
\deftab560
\pard\pardeftab560\slleading20\partightenfactor0

\f0\fs26 \cf0 git branch <new-branch-name>\
git checkout <other-branch>     (or)   git switch <other-branch>\
git branch -m <new-name>  (Rename current branch)\
git branch -m <old-name> <new-name>  (Rename any local branch)\
git push origin \'97delete <old-name> (First, delete the current/old branch)\
git push -u origin <new-name>  (Then, simply push the new local branch with the correct name)			\
git push -u origin <local-branch>   (Uploading a local branch for the             first name)     \
git branch \'97track <new-branch> origin/<base-branch> (or) git checkout \'97track origin/<base-branch>\
git pull  (Pulling the latest changes from remote repository)\
git push (Pushing the latest changes to remote repository)\
git branch -v (Seeing the status of each branch)\
git branch -d <branch-name> (before deleting switch to another branch)\
git push origin \'97delete <branch-name>\uc0\u8232 \
Merging Branches\
	git switch main (Checkout the branch that should receive the changes)\
	git merge <branch-name> (Execute merge command with the name of the branch that contains the desired changes)\uc0\u8232 \
Alternate way to integrate changes from another branch into your current local HEAD branch\
	git switch main (Checkout the branch that should receive the 	changes)\
	git rebase <branch-name> (Execute merge command with the name of the branch that contains the desired changes)\
}
